# ⚽ Football Edge — MVP

- 1 pari conseillé par match (1X2 / O-U 2.5 / tirs équipe / corners / tirs joueur).
- Combiné du jour (cible 3.5–7.0).
- Auto-refresh des données de démo à l’ouverture.

## Lancer
```bash
pip install -r requirements.txt
streamlit run app/main.py
